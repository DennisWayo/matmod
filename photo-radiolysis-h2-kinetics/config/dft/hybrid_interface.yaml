base_config: base_dft.yaml

metadata:
  system: hybrid_interface

interface:
  fragment_variants: [undoped, yb, ybtm]
  default_fragment_variant: ybtm
  placement_options: [n_rich, ring_center, bridge, defect_adjacent]
  separation_values: [2.4, 2.8, 3.2]
  vacuum: 14.0
